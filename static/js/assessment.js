(() => {const steps=[...document.querySelectorAll('[data-step]')];if(!steps.length)return;let current=0;const next=document.querySelector('[data-next]'),prev=document.querySelector('[data-prev]'),submit=document.querySelector('[data-submit]'),bar=document.querySelector('[data-progress]'),label=document.querySelector('[data-progress-label]');function show(){steps.forEach((s,i)=>s.classList.toggle('active',i===current));prev.style.display=current?'inline-flex':'none';next.style.display=current<steps.length-1?'inline-flex':'none';submit.style.display=current===steps.length-1?'inline-flex':'none';bar.style.width=((current+1)/steps.length*100)+'%';label.textContent=`Step ${current+1} of ${steps.length}`}next.addEventListener('click',()=>{if(current<steps.length-1){current++;show()}});prev.addEventListener('click',()=>{if(current>0){current--;show()}});show()})();
